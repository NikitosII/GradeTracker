# Auto-provisioned on Grafana startup so the Prometheus datasource is wired up
# with no manual clicking.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
